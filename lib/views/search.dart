import 'package:flutter/material.dart';

class Search extends StatelessWidget {
  final double value;
  final bool isDarkMode;

  const Search({Key key, @required this.value, @required this.isDarkMode})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
