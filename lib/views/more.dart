import 'package:flutter/material.dart';

class More extends StatelessWidget {
  final double value;
  final bool isDarkMode;

  const More({Key key, @required this.value, @required this.isDarkMode})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
