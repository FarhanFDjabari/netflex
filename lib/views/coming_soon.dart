import 'package:flutter/material.dart';

class ComingSoon extends StatelessWidget {
  final double value;
  final bool isDarkMode;

  const ComingSoon({Key key, @required this.value, @required this.isDarkMode})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
